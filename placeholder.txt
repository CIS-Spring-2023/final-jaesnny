   elif table == 'o':
        # outputs from fish table all records
        output_all = "select * from fish"
        mycursor.execute(output_all)
        fishes = mycursor.fetchall()
        # formats rows and prints
        for row in fishes:
            print("Id:", row[0], "Superclass:", row[1], "Species:", row[2], "Color:", row[3], "Acquired:", row[4], "Alive:", row[5], "\n")
    # quits
    elif table == 'q':
        break
    else:
        print("That is not a selection. Please try again.")
connection.close